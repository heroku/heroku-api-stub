#!/usr/bin/env ruby

require "multi_json"
require "rack/instruments"
require "sinatra/base"

require_relative "../lib/doc_stub"

abort("usage: docstub <file>") unless ARGV[0]

doc = MultiJson.decode(File.read(ARGV[0]))
app = DocStub::Generator.new(doc).run

app.run! port: 3000
